The ____ operator is used to test whether the object is an instance of the specified type (class, subclass or interface).

instanceof



Using a parent class instance, how we can access to child class methods.

((ChildClass) instanceParentClass).childClassMethod();



In an abstract class thee methods an attributes that aren't abstract has to use the modifier ____.

Protected.



